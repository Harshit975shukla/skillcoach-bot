"""Retired reminder adapter: deliberately sends no duplicate daily notifications."""


def main():
    print(
        "Legacy reminders are disabled. Actions owns coaching schedules; use skillcoach.cli recover for retries."
    )


if __name__ == "__main__":
    main()
