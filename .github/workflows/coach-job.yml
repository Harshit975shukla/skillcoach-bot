name: Shared private coaching worker
on:
  workflow_call:
    inputs:
      kind:
        required: true
        type: string
      intended_date:
        required: false
        type: string
        default: ""
      notify_owner:
        required: false
        type: boolean
        default: false
      verify_media:
        required: false
        type: boolean
        default: false
permissions:
  contents: read
  actions: read
concurrency:
  group: skillcoach-private-worker-${{ inputs.kind }}
  cancel-in-progress: false
jobs:
  coach:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install shared Python runtime
        run: python -m pip install -r requirements.txt
      - name: Check for ready media work
        id: media
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_CA_CERT_FILE: ${{ vars.DATABASE_CA_CERT_FILE }}
          OPERATION: ${{ inputs.kind }}
          VERIFY_MEDIA: ${{ inputs.verify_media }}
        run: |
          if [ "$VERIFY_MEDIA" = "true" ] || [ "$OPERATION" = "lesson" ] || python -m skillcoach.cli needs-media; then
            echo "needed=true" >> "$GITHUB_OUTPUT"
          else
            RESULT=$?
            if [ "$RESULT" != "3" ]; then exit "$RESULT"; fi
          fi
      - uses: actions/setup-node@v4
        if: steps.media.outputs.needed == 'true'
        with:
          node-version: "22"
      - name: Install local renderers only when needed
        if: steps.media.outputs.needed == 'true'
        run: |
          npm ci
          if ! command -v ffmpeg >/dev/null; then
            sudo apt-get update -qq
            sudo apt-get install -y ffmpeg
          fi
          if ! command -v espeak-ng >/dev/null; then
            sudo apt-get update -qq
            sudo apt-get install -y espeak-ng fonts-dejavu-core
          fi
      - name: Verify the production runner's offline narration and video tools
        if: inputs.verify_media
        run: python tests/verify_storytelling.py
      - name: Capture intended run timestamp
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          CREATED=$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" --jq .created_at)
          echo "RUN_CREATED_AT=$CREATED" >> "$GITHUB_ENV"
      - name: Execute explicit operation and recover pending work
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_CA_CERT_FILE: ${{ vars.DATABASE_CA_CERT_FILE }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          OWNER_ID: ${{ secrets.OWNER_ID || secrets.CHAT_ID }}
          TELEGRAM_BOT_USERNAME: ${{ vars.TELEGRAM_BOT_USERNAME }}
          PRIVATE_DASHBOARD_URL: ${{ vars.PRIVATE_DASHBOARD_URL }}
          MAX_LEARNERS: ${{ vars.MAX_LEARNERS || '10' }}
          DAILY_AI_OPERATIONS: ${{ vars.DAILY_AI_OPERATIONS || '40' }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_MODEL: ${{ vars.GROQ_MODEL || 'openai/gpt-oss-120b' }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL || 'gemini-2.5-flash' }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          DASHBOARD_REPO: ${{ vars.DASHBOARD_REPO }}
          DASHBOARD_PATH: ${{ vars.DASHBOARD_PATH || 'docs/data.json' }}
          DASHBOARD_URL: ${{ vars.DASHBOARD_URL }}
          OPERATION: ${{ inputs.kind }}
          INTENDED_DATE: ${{ inputs.intended_date }}
          MEDIA_READY: ${{ steps.media.outputs.needed }}
          NOTIFY_OWNER: ${{ inputs.notify_owner }}
        run: |
          export PATH="$PWD/node_modules/.bin:$PATH"
          MEDIA_ARGS=()
          if [ "$MEDIA_READY" != "true" ]; then MEDIA_ARGS+=(--no-media); fi
          if [ "$OPERATION" = "recover" ]; then
            if [ "$NOTIFY_OWNER" = "true" ]; then
              python -m skillcoach.cli announce-ready --release "$GITHUB_SHA"
            fi
            python -m skillcoach.cli recover "${MEDIA_ARGS[@]}"
          elif [ -n "$INTENDED_DATE" ]; then
            python -m skillcoach.cli schedule "$OPERATION" --date "$INTENDED_DATE" "${MEDIA_ARGS[@]}"
          else
            python -m skillcoach.cli schedule "$OPERATION" --at "$RUN_CREATED_AT" "${MEDIA_ARGS[@]}"
          fi
