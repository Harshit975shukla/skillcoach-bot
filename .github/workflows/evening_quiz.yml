name: Evening quiz (18:00 IST weekdays)
permissions:
  contents: read
  actions: read
on:
  schedule:
    - cron: "30 12 * * 1-5"
  workflow_dispatch:
    inputs:
      date:
        description: "Intended IST date YYYY-MM-DD (blank uses run creation date)"
        required: false
jobs:
  quiz:
    uses: ./.github/workflows/coach-job.yml
    with:
      kind: quiz
      intended_date: ${{ inputs.date || '' }}
    secrets: inherit
